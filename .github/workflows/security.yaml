name: Security Audit

on:
  pull_request:
    branches: [main, master]

jobs:
  audit:
    uses: haidaralimasu/thirdgen-scanner/.github/workflows/scan.yml@main
    permissions:
      contents: read
      pull-requests: write
